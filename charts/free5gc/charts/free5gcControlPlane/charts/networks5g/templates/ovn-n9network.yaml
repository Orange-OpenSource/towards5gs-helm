{{- if .Values.ovn4nfv.enabled }}
apiVersion: k8s.plugin.opnfv.org/v1alpha1
kind: Network
metadata:
  name: {{ .Values.ovn4nfv.n9network.name }}
spec:
  cniType : ovn4nfv
  ipv4Subnets:
  - subnet: {{ .Values.n9network.subnetIP }}/{{ .Values.n9network.cidr }}
    name: subnet1
    gateway: {{ .Values.n9network.gatewayIP }}/{{ .Values.n9network.cidr }}
{{- end }}
